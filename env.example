# OpenAI-compatible local runtime (vLLM)
OPENAI_BASE_URL=http://localhost:8000/v1
OPENAI_MODEL=gpt-oss

# Optional: API key not needed for local vLLM, but some clients require a value
# OPENAI_API_KEY=EMPTY

